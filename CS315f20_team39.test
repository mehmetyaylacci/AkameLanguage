
bool operationExample(int a) {

    int c, d, e;
    float a = 5.2;
    float b = 5.35;
    for( int i = 2; i < 5 ) {
        output("i");
    }

    bool e;

    i.toggleCamera();
    input(e);

    return e;

}

#"if temp is higher than 90, output warning message"#
int overheat( int temp ) {
    if( temp > 90 ) {
        output("HIGH TEMP WARNING!");
    } else {
        output("soguk");
    }
    return temp;
}

#"check temp"#
while( true ) {
    a.readTemp();
    int temp = a;
    int temp2 = temp;
    b.readInclination();
    int incl = b;
    a.toggleCamera();
    if( temp == temp2 ) {
        while( incl != 0 ) {
            output("ucuyor valla");
        }
    } else {
        while( incl > 1000 ) {
            output("goklerin fatihi");
        }
    }

}
